@extends('app')

@section('content')
	<h1>Agenda</h1>
	
	@if ( !$poblacions->count())
                No tens poblacions!
        @else
                <ul>
                        @foreach( $poblacions as $poblacion )
                                <li><a href="{{route('poblacions.show', $poblacion->nom) }}">{{ $poblacion->nom }}</a></li>
                        @endforeach
                </ul>
        @endif
@endsection
